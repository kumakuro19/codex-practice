# Project rules

- Target: simple static website (HTML/CSS/JS)
- Keep it lightweight (no frameworks)
- Create separate files: index.html, styles.css, app.js
- Use semantic HTML and responsive layout
- Do not add build tools
- If you change file names, update all references
